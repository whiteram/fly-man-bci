# 项目进度总览（STATUS）

更新：2026-09。承接 HANDOFF.md（交接/环境/数据）与 docs/TECHNICAL.md
（方法学细节）；本文回答"现在做到哪了"。

---

## 1. 一句话现状

果蝇视觉系统（MaleCNS v1.0 连接组，区域可选装配）在自然视觉刺激下的
群体放电仿真已全链路打通，并经四球壳人脑前向核投影为头皮脑电，配有
完整 3D 可视化、交互式标定体系和多套电极配置；刺激支持合成协议与
**真实视频输入**（灰度→小网膜采样映射，`visual_inputs.json` 目录 +
页面同步双视图），其他模态的扩展路径见 docs/STIMULUS.md §6。

## 2. 已完成的仿真阶段

| 阶段 | 状态 | 产出 | 代码/实验 |
|---|---|---|---|
| 数据管线（连接组下载/子网/注释） | ✅ | 视觉子网 + 注释 + 递质 + 坐标 | scripts/, docs/data.md |
| ERG 前向（光转导→R→板层→眼表面电位） | ✅ | 与实测 ERG 波形对标 | exp001–002 |
| ON/OFF 不对称、运动 gratings | ✅ | 方向响应基线 | exp003–004 |
| T4/T5 方向选择机制化 | ✅ | Hex 轴 DS 电路（T4/T5 preferred −135°） | exp005–006, exp013 |
| 头内"果蝇 EEG" + 导体模型 | ✅ | 密封二球核 SealedHeadPairField | exp007–008 |
| 板层极性/电流模式修正 | ✅ | RL/LM 电流基 + 机制化 LMC | exp009, exp013 |
| 人比例思想实验（×400 装入四球壳） | ✅ | FourSpherePairField + 自适应 scale | exp010 |
| 刺激检测论（信号 vs 背景脑电） | ✅ | T7 d′≈2（约 46 次试验） | exp011 |
| 返回约束/保守性检验 | ✅ | — | exp012 |
| 双侧视叶 + VPN→中央脑 + 全 CNS 装配 | ✅ | 区域可选装配（regions 开关）；全 CNS 150,601 神经元仿真（全部 Traced∩有 soma 的三大超类，覆盖口径见 HANDOFF §4.3） | exp014–017, regions.py |
| 头皮电极阵列扩展 | ✅ | 45/64/128/EGI241 四套配置（elec_configs.json） | make_elec_configs.py |
| **计算加速：numba + GPU + 分级缓存** | ✅ | numba 内核 2.38×；`--gpu` 循环 47 min→156 s（轨迹逐位一致）；电路/前向核双缓存（换电极配置只重建核） | docs/ACCELERATION_PLAN.md P2/P3/P5 |
| 交互标定体系 | ✅ | 10-20 手柄标定（ni/th/yaw/roll）、视角标定、人头形态校准 | viz/index.html |
| 可视化页面 | ✅ | 电极名称标签、热图、时段色条、配置切换、预览波形 | viz/index.html |
| **头皮脑电 EDA（分布/通道差异/刺激可解码性）** | ✅ | 干净信号带通 0.5-6 Hz 解码 CV R²=0.84、级联滞后 23/17 ms、空间参与秩≈2；频带分布 δ 主导（0.1-4 Hz 占 72%，无 α 峰，AC 幅度比人脑背景小 ~130×）；前提=果蝇替代人脑（仅设备噪声）：0.1-1 Hz + 主成分空间滤波单段 SNR≈1.2，~3 段平均到 SNR 2 | exp018 |
| **BCI 范式采集分支（SSVEP-Benchmark）** | ✅ | 对齐 Wang 2016（40 类 8-15.8 Hz）；多种子 40 类 × 5 重复：CCA top-1 干净 72% / 缩放假想设备噪声 83%（随机 2.5%，与人类基准同量级）；平均不提精度——瓶颈是 0.2 Hz 类间距可分性而非噪声；SSVEP 频段响应带内 SNR ~27 dB——低频主导是刺激属性而非模型属性 | bci/ssvep_benchmark |
| **BCI 范式：运动想象 / 嗅觉 / 听觉** | ✅ | `motor_imagery`（2 类，复用 exp020 MI-v2）：想象窗空间 LOO 100%（p=0.003）；`olfactory`（3 气味身份，exp019 电路，ORN 群大小补偿幅度）：3 类 89%（p=0.0005，DA1/DM2/VA1v 空间模式可分）；`auditory`（新区域 `audition`：JO-A 115→JOA_C 1,578 对/24,392 突触→中央脑；求偶歌包络建模）：4 类 54%（p=0.007）/ 侧别 75% / 模式 75%——类间总 rms 相同，可分性全在空间模式；`cvep`（对齐 BETA，127 位 m-序列 40 循环移位码）：**40 类模板匹配 100%（p=0.0005，chance 2.5%）**；`looming` 3 类 72%（运动 vs 静止 100%、膨胀方向 58%≈随机——方向选择性不外显于头皮）；`direction` 4 类 46%（p=0.022，时间形状模板；空间 rms 42% 不显著）；`mrcp`（LAL 阶梯坡道+PRO 执行）：**运动前坡道窗 L/R 100%（p=0.0035）**，坡道爬升期意图先于执行可解码；附带修复 chem_fn 同 pop 多通道覆盖 bug（改累加，旧协议等价）；`seizure`（`no_cen_damp` 退出安全工作点，点火=发作）：发作 vs 健康 100%（p=0.0025）、灶侧别 100%（p=0.026），并发现**自发癫痫生成**（默认增益下纯噪声 ~0.55 s 自燃，早于焦点驱动）；`touch`（新区域 `touch`：TC 2,558→CEN 17k 突触）：触压侧别 2 类 100%（p=0.0035）；`gustatory` 3 器官身份 39% 不显著——阴性，GRN_C 8.4k 对结构上不可见（exp019 结论复现）；平衡棒构建器被数据否决（201/205 已在 PRO 内）；**修复 pipeline `_indices` 密集查找表 OOM**（TC 5e9 带 37 GiB；改 searchsorted，全运行省数 GB）；round-4：`p300`（外周适应 oddball）目标偏差响应 6×（AUC 1.00，p=2e-4，诚实框架=适应非认知）；`cnv`（S1→LAL 坡道→S2 双刺激）预期窗 L/R 100%（p=0.0035）；`auddev` 包络速率偏差阴性（适应跟踪均值、不敏感速率——机制方向）；癫痫**增益扫描**：0.002–0.0035 全健康、0.004 自燃——0.0035→0.004 硬分岔，无可控中间增益，可控性=侧别非起始；`odormix`：混合非线性 AB/预测=0.80（次线性 20% 压制）+ 身份/顺序解码均 100%（依赖 chem 累加修复）；`dose` 浓度 3 档 100%（Hill 型饱和曲线）；`hybrid` SSVEP×想象：想象流 100% 但视觉频率线被中央 OU 背景掩没 ~500×（CCA/PSD/空间滤波皆不可恢复——极端双任务干扰，诚实记录） ;round-6：`stereo` 立体嗅觉（ORN rootSide 侧别组）2 类 100%（p=0.0035）；`visualfield`（新 eye_map half_split 单眼半帧几何）2 类 100%（p=0.0035）——空间模式是最稳健读出轴；`arousal` OU 噪声状态轴**阴性+机制发现**：自发活动由偏置电流驱动（噪声×0.05 底噪仅 −0.8%），真状态轴需基线电流调制=机制方向 ;第三梯队机制①：**突触短时抑制 STD**（Tsodyks-Markram 资源门控，spec/`--std-gates` opt-in，CPU+GPU 双路径，耗竭在释放时刻、经延迟环生效，STD-off 位级不变）；`mmdev` 身份偏差列车：重复抑制 A5/A1=0.02（主导=级联适应）、**偏差比 devB/A5≈3**、STD 边际贡献 +16% ;机制②：**工作点状态轴**（`--base-scale` 偏置电流标度）——`gainstate` 显示底噪/刺激增益/响应可靠性三者随工作点移动（气味响应 2.4×，低工作点进入近阈随机区，试次方差 140×）——状态与增益共用一个旋钮（增益调制定义，不可分） ;机制③：**DAN 门控 KC→MBON 可塑性**（`--plastic-mb` 拆 33,398 对可塑组 + `--plastic-window/lr` 三因子抑郁规则，CPU+GPU，plast-off 位级不变）——`condit` 驱动条件化：**学习签名 +105%（p=0.026，头皮可见）**，MBON 去抑制方向机制一致；附带发现 PN→KC 通路被 chem 工作点压制（气味身份到不了 KC，真气味条件化=AL 增益独立化机制方向）；排查并修复 GPU trial.run 漏传 mod_fn | bci/*, exp021-022, docs/SENSORY_INPUTS.md §4-5 |
| **跨试次记忆 + 获得-消退曲线** | ✅ | 机制④：KCM 可塑权重**跨试次持久**（`--plastic-state-in/out` 状态链；边身份 md5 校验和——突触内序为表内容的确定函数，错配即拒载；elig 试次间清零=ISI≫400ms）+ **慢同稳态恢复** `--plastic-tau-w`（w+=(1−w)dt/τ，门控无关，0=冻结、位级不变，CPU+GPU `k_plast_adv` rec 参数）；`condit2` 获得-消退范式（5s 试次逐探针：获得 10×奖励→消退 8×撤奖→never-rewarded 对照 8×，各 2 seed，52 试次）：**获得斜率 +0.0053 µV/试次（perm-p=0.0002，单试次跳变+平台，w→0.15 平衡点）、消退 −0.0142（p=0.0002，探针 −37% 回落，w 0.28→0.78）、对照平坦（p=0.30 n.s.，w 恒 1.00）**，early→late 配对 vs 对照 +0.033/+0.001 µV——项目第一条**逐试次学习曲线**；诚实边界：强化门仍是时间窗 proxy、恢复为同稳态非反 Hebbian；附带重生成 docs/PARAMS.md（5 个区域基流参数遗留失配）| bci/condit2, src/ffbm/{simulation,gpu,pipeline}.py, viz/export_data.py |
| **AL 增益独立化 + 注意增益调制 + 噪声稳健性表** | ✅ 机制⑤⑥ | ①`--al-gain`：把 AL→KC（19,511 对 PN→Kenyon）从 CEN_C 拆出独立增益组 ALK，绕过 chem 工作点 500× 压制——**真实气味（ORN→PN→KC）第一次驱动 KC**（elig 31 vs 对照 0.84）；校准无干净稳定窗（持续分量随增益连续，对照自身 +0.44µV 平台），选 g=0.01；`condit3` 真气味条件化（lr 重标定 5e-5，w→0.94/驱动子集 0.34）：**诚实阴性**——训练窗/探针窗头皮响应 learn=nolr（外周 ORN/PN 全增益主导，读出腿 KCM 0.002 弱 500×）；输出腿前沿三点（--gain-scale KCM ×50/×250）：×50 安全但 +5% 边缘、×250 可见（train ×0.5）但 nolr 闩锁——**可塑性=自动闩锁保护**（learn 臂自我抑郁回落稳定），表达-稳定权衡就在点火边界；②`--gain-scale 组=倍率` 运行时通路增益调制（g_unit 运行时量，不动缓存）：`attend` 注意实验（OLR_V ×4/×16）：**无法恢复掩没视觉线**（SNR 0.72→1.17，需 ~25dB）、想象流零代价（8/8 全臂）、**hybrid 掩没归因更正**（nwp 对照臂：工作点 ×1.0、双任务 ×1.4，~300× 来自中枢区域纳入本身——hyb_n 漏传 chem 无测量影响，hybrid README 已附更正）；③`noise_robustness` 纯分析表（15 范式×5 档信号级白噪声+统一空间 rms LOO）：空间签名范式 r=10 仍 100%（窗积分压白噪声），cvep 空间 rms 立即失效（信息在时间码） | bci/condit3, bci/attend, bci/noise_robustness, viz/export_data.py |
| **阻断检验 + 动机门控学习** | ✅ | ⑦**Kamin 阻断检验**（`bci/blocking`，KC 子类型=合成气味 A=KCg-m/B=KCab-m/C=KCab-s，状态链跨阶段、权重冻结，40 试次）：**无阻断**——A+ 预训练对 B 在 AB+ 的学习零影响（终态 w_B 两条件同触底 0.0200，diff +0.0000、perm-p=1.0；头皮 B 曲线差 −0.003µV、p=0.66）——纯三因子规则（elig×mod 无误差项）的类属边界确认，预测误差门（mod ∝ 实得−预测）列为新机制方向；副发现：开窗期自发 KC 活动的**背景泄漏**（未训练子类型 10 试次 w→0.94）；状态文件新增逐边 `pre/pre_type`（w 可按子类型分解）；⑧**动机门控**（`--plastic-mod-scale` 门强度轴 + `bci/motivate`，20 试次单发剂量-反应）：**完美单调**——mod 0.125→1.0：w 0.855→0.286、探针增量 +0.037→+0.126µV（Spearman ρ=±1.0，p=0.08=4 臂排序检验理论下限，臂内 sd≤0.003 近确定性）——动机/奖励强度轴线性进入学习深度，与饥饿果蝇条件化增强同构；附带⑨**APL 反馈环手术**（`--mb-mod-gain` 拆 KC↔APL 8,304 对 GABA 负反馈环独立增益；DPM 多巴胺能正环设计排除，实测混入 nolr 780µV）：四点表征**诚实阴性**——轨迹为纯直流不动点（非振荡），APL 加性反馈只能移动不能消除（KCM×250 下闩锁加深 131-771µV；KCM×50+g0.05 有 +73% 训练签名但双臂坐抬高平台=混杂）；三因子可塑性仍是唯一有效稳定器；除性分流实现（瞬时电导几何）列为新立项 | bci/blocking, bci/motivate, viz/export_data.py |
| **短/长时记忆分离 + 温湿度模态** | ✅ | ⑩**双时间常数区室**（`--plastic-dual-tauw 30000,300000`：KCM 按突触前 KC 子型拆 KCMg 14,298 对/KCMab 19,100 对，独立恢复，专属盐 +plmb2 防拓扑缓存碰撞；状态文件双组 w_<grp> 格式+旧单组兼容）：`dualmem`（每试次双探针 γ/αβ 窗，4 session×16 试次）——机制层消退后 **γ w=0.79（近遗忘）vs α/β w=0.15（保持 5.3×）**（逐位一致确定性）；头皮层 γ 消退 −43% vs α/β −19%，4 session 全同向，斜率差置换 p=0.121=**该检验精确下限**（2/16）——瓶颈是 session 数非效应；⑪**新模态区域 thermal**（exp023：TH 25 TRN + HY 66 HRN 双 pop，TH_C 1,257 对/45k 突 + HY_C 2,524/46k → CEN；3s 慢斜坡）：`bci/thermal` **温度 warm/cool 空间 LOO 92%（p=0.017，整体 rms 平坦——又一空间模式轴证据）**；湿度 dry/moist 58% 阴性（干/湿中央靶点重叠更高的结构假设） | bci/dualmem, bci/thermal, exp023_thermal, viz/export_data.py |
| **预测误差门 + 模型-生理验证表 + 睡眠探查** | ✅ | ⑫**误差门阻断**（`bci/blocking2`，Rescorla-Wagner 实验层门：error=R−ΣV，α=0.3，V 按 session 持久化，经 --plastic-mod-scale 注入、突触规则零改动，40 试次）：**阻断涌现**——w_B blocked 0.5997 vs control 0.1255（对照多学 4.8×），V 轨迹教科书式（V_A→0.88→AB+ 误差 0.12）；与 bci/blocking 合并：**阻断是门的计算类性质而非突触性质**（窗门无阻断/误差门阻断，同一规则）；⑬**验证表**（docs/VALIDATION.md）：KC 自发率 **0 Hz vs 文献 ~0.004 Hz 匹配**（稳态 elig 探针；顺带修正背景泄漏解读=网络驱动非自主噪声）、KC 诱发率偏高 1-2 量级（机制解读=与 APL 弧线同一稀疏化缺失）、嗅觉起始延迟 126ms 量级一致、ERG 沿用 exp001；⑭**睡眠双稳态探查**（60s×3：cen_gain 0.004+STD 门控=全程下行零转移；sz_l 起步上行+U=0.02/0.1=持续 10.5µV 平台且与 U 无关）——双稳未达成，**上行态维持路径不走 CEN_C 复归**（候选：噪声驱动群体发放）列为机制问题；附带：单组盐 +plmb1（dual pilot 曾污染 +plmb 键）、`--mb-mod-tau`（tau 3ms 仍闩锁，APL 三变体穷尽） | bci/blocking2, docs/VALIDATION.md, viz/export_data.py |
| **上行态归因 + PN 放大验证（含符号潜伏缺口修复）** | ✅ | ⑮**上行态维持者鉴定**（`bci/sleep2`，机制 14 后续）：--gain-scale 逐通路零化扫描（6×20s）**推翻原推断**——维持者= CEN_C 复发（全增益 0.004；零化后平台 10.35→1.39 µV 塌缩、全 class<1Hz），OLR/视觉/ORN 前馈零化平台不变（10.25-10.63）；机制 14 的 U 无关性重解读=当时参数化的 STD 门未咬住维持电流（候选：τ_rec 太快秒级回满）→ 睡眠双稳态可行路径=秒-十秒尺度慢变量（τ_rec≥5s 资源门或自适应 i_base）；⑯**PN 层弱信号放大**（`bci/pnrate`，VALIDATION #5 关闭 ⏸→✅）：`--pop-rate` 分群速率+ `--chem-amp-scale` 剂量扫描（ORN_DA1 ×{0.25..4}×2 种子）——PN/ORN 诱发率比 **15.1×（阈上最低档）→3.2×（4×）**，PN 饱和 ~22 Hz，**扩张非线性定性匹配 Bhandawat 2007**（实现源=30:1 汇聚+LIF 阈值整合，非树突机制，与 #3 稀疏化缺失互为镜像）；附带发现：**extra-edge 符号潜伏缺口**（sign 列从未传入突触池，中枢全兴奋运行 vs exp017 README 声明）——MBMD 以 use_sign 定向修复，全局启用=独立立项 | bci/sleep2, bci/pnrate, docs/VALIDATION.md, src/ffbm/pipeline.py |
| **误差门电路化（机制 15）** | 🟡 | ⑰**dangate**（`bci/dangate`）：Rescorla-Wagner 误差项从实验层搬进电路——KC→MBON 电位化读出（负 lr+w0 基线；k_plast_adv 双分支+镜像 CPU 实现+5 测试）→ MBON→DAN 349 条 GABA 边分裂 MBMD（绝对增益+use_sign）→ 奖励与 V 同作用 DAN_err 分室（154 细胞）→ mod=clip((r−r0)·g,0,1) 逐时间步读上一步放电（因果）。校准三发现：KCM g_unit 被 chem 工作点静默覆盖（修复）、混合符号在电导模式下净兴奋（驱动力 55 vs 25 mV，弃 244 条兴奋行=最小负反馈架构）、MBON 二值工作点（kcm 0.015+w0 0.03 定位开关两侧）。全量（2×2×10 试次，lr −2e-7）：**电路级阻断涌现但弱**——w_B blocked 0.267 vs control 0.296（1.11×）、误差压制 12%、早期分离 55%（control p2 首试 1320 vs blocked ~850）；**分室特异误差编码**：control p1（C+）误差平线 1950（V_C 不入分室）。与 blocking/blocking2 合并：窗门 0× → RW 门 4.8× → 电路门 1.11×——阻断随实现层级变弱不消失，弱源=MBON 二值性（门地板 41%）；增强路径=MBON 超极化偏置/降奖励幅度（新立项）| bci/dangate, viz/export_data.py, src/ffbm/{simulation,gpu,pipeline}.py |
| **MBON 余量恢复（机制 15 增强）** | ✅ | ⑱**dangate2**（`bci/dangate2`）：诊断 dangate 瓶颈为**抑制传递饱和**——未训练基线 MBON（~43 Hz）已把 MBMD 推满，blocked/control 的 DAN 轨迹几乎相同（8.5/8.8 Hz），门在 V 分化前饱和；修复=`--mbon-bias`（MBON* 紧张性超极 chem 通道，96 细胞，运行时幅度不进缓存键，~20 行）。静态传递链标定（scan_summary.json）：bias −300 pA 把未训练点压到 w-膝下——r 对比 **51/10 Hz ≈5×（bias 0 时 1.05×）**，门增益 0.05→0.02 配平；剂量 >150 pA 前平坦（兴奋驱动远超偏置）、−600 整条曲线右推丢训练端。全量（2×2×10）：**阻断比 1.11×→1.41×、误差压制 12%→33%、早相分离 1.8×**，且首次出现**电路内 RW 递减**（blocked p1 mod 1808→792、control p2 1778→646——误差随 V 逐试次收缩，无实验层参与；blocked p2 门稳于 mod≈0.32=w 增长/恢复平衡点）；诚实记录：control p1 的 C 不入分室沿袭 ⑰（control=全新 AB+，Kamin 设计仍成立）、与实验层 4.8× 的剩余差距=单室误差求和 vs 真实逐室架构+恢复项设定平衡点 | bci/dangate2, viz/export_data.py |
| **慢变量睡眠双稳态 + STD 恢复盘 bug 修复** | ✅ | ⑲**sleep3**（`bci/sleep3`）：按 sleep2 归因把慢 STD 只挂 CEN_C（τ_rec 秒-十秒尺度），U 扫描 0.001→0.2 全部与对照逐位相同 → 深挖发现**潜伏计算 bug**：`k_std_rec`（GPU）与 CPU 孪生的恢复公式 `d += (1−d)·rec`（rec=exp(−dt/τ)≈1）= **每步瞬时回满，STD 在任意 (U,τ) 下都是无效门**；修复为 `·(1−rec)`（每步恢复 dt/τ，时标 τ），逐脉冲探针验证 d 0.1→0.01→0.001、步间恢复 0.0023%=dt/τ 精确吻合，55/55 测试通过。**该 bug 即机制 14「STD 门从未咬住维持电流」的真相**（非动力学原因）；历史影响：mmdev 的 std_on/off 对照及 8e42dde 前所有 --std-gates 结论需重审。修复后剂量地图（60s 双脉冲迟滞设计 sz_l2，level 10-30s）：τ=5s 下 U≤2e-4 为**耗竭上行态族**（graded 不动点 4.66→3.25 µV）、U≥3e-4 塌缩（驻留 3.5→2 s）、2.5e-4 边界态（塌缩+下态噪声再燃）；**U=1.5e-4、τ=10s 涌现完整慢振荡循环**：上行 ~5s（MBON 225→80）→塌缩→下行 ~28s（≈3τ 再武装）→第二次点火再燃→再塌缩——sleep2 归因+修复后慢 STD=最小慢负反馈，上行态自身放电耗竭其维持电流（d_eq=1/(1+U·r·τ)，维持阈值由增益二分定位在 0.5-1×0.004）；诚实记录：再点火依赖外脉冲（自发翻转未观察）、下态带 ~2.3µV 残余、门为群体粒度 | bci/sleep3, src/ffbm/{gpu,simulation}.py, viz/export_data.py |
| **mmdev 重审（STD bug 后果闭环）** | ✅ | ⑳**mmdev 重跑**（`bci/mmdev`）：**撤回旧「STD 边际贡献 +16%」**——bug 下两条件差异仅来自 f32 末位混沌+种子抽样；修复后 std_off 位级复现旧值（2.74 ✓），std_on（原参数 U=0.25）整个 ORN→CEN 通路被压垮（A1 0.207→0.0006 µV = 300×，重复抑制 0.02→0.48 = 真实突触重复抑制显现，deviance 7.83 由分母塌缩驱动）。U 剂量补充（0.05/0.1×2 种子）：A1 保持（0.200/0.172）但**偏差脉冲自耗竭**（600 pA 4× 强 → 脉冲内 d_eq 掉更深 → devB 7× 压低 → deviance 0.78/−0.62）——**STD=强度依赖增益控制**的新机制定性（对强刺激自衰减，与真实 ORN→PN 增益控制一致），但与 oddball 范式「偏差=最强脉冲」的设计冲突：后续等强度偏差设计才能干净分离新异性与强度；诊断 dump：活跃 ORN 边等效放电率仅 ~3 Hz（与 pnrate 低 ORN 速率一致） | bci/mmdev |
| **等强度/等响应偏差 + DC 状态开关** | ✅ | ㉑**mmdev2**（`bci/mmdev2`）：按重审预告执行干净偏差设计，两步收口：①**等强度**（`mmn_match`，DM2@150）——偏差脉冲头皮完全不可见（devB −0.0008±0.0004 µV，devB/A1≈−0.01，三条件同；旧 2.74 偏差比全由 4× 强度承载）；solo 剂量标定（`da1_solo`/`dm2_solo`+`--chem-amp-scale`）测得 **DM2 阈值崖 466–492 pA**（466 双峰 0.027/0.090 崖区不可靠、492 稳定平台 +0.247±0.009 vs DA1@150 +0.197=**校准比 1.25**）；②**等响应**（`mmn_matchr`，DM2@492）——oddball 内 devB +0.0095±0.0005，比 solo 标定低 **~25×** → 预登记三向判读落在**交叉适应**：标准列车的适应跨气味身份传递（共享下游底物）。**意外主发现：DC 状态开关**——所有 oddball net 量骑在 +0.44 µV 持续平台上（mean −1.389→−1.825、std 0.10→0.18）：任何有效单脉冲（DA1@150、DM2@466/492）触发同一满幅 **−0.430±0.002** 台阶（开关阈值≪快响应阈值——DM2@466 快响应仅 +0.059 却满台阶），单脉冲即饱和，其后 A5/devB（150 与 492 pA 逐位同）/recA 台阶 ≤0.004=地板，**身份盲+幅度盲**；U=0.05 无影响、U=0.1 连开关一起摧毁（A1 台阶 −0.107±0.188 双峰，与快响应同走 ORN→CEN）；**8 s 零恢复**（`da1_dc8` 双种子全程 −1.820±0.002 平坦，τ>8 s 准永久翻转）——嗅觉头皮信号的主导慢分量（3× A1 快响应）。**结论：无 MMN**（快增量/DC 台阶均无身份新异性；mmdev「身份偏差响应成立」一并撤回），oddball 偏差比=强度伪影+分母塌缩+地板噪声；DC 机制定位、交叉适应底物、DC 衰减 τ=后续方向 | bci/mmdev2 |
| **慢振荡内源性检验 + τ≥15s 驻留** | ✅ | ㉒**sleep4**（`bci/sleep4`）：关闭 sleep3 的诚实注记（再点火靠外部脉冲）。**Arm1 内源性检验**（`sz_l1` 单脉冲 90s，U=1.5e-4/τ=10s 双种子）：[40,90]s 无脉冲窗（塌缩 ~8s + 3τ=30s 再武装后）**零自发再点火**（最大 2.36 µV，MBON 3–5 Hz vs 对照闩锁 388 Hz，≥8τ 时程）→ **慢振荡外源驱动直接证实**（噪声单独无法越过闩锁阈）；附带确定性验证：sz_l1_s616 前 60s 与 sz_l2_s616 逐位相同；trace 精化：慢 STD 剂量下下态头皮签名是 ~2.3 µV 平台（ALPN 38 Hz 残余托高，MBON 3 Hz=电路真下态），非高 U 的 1.4 µV 深塌缩；上行相实为 1–2 s 瞬态（MBON 157 Hz 衰减）。**Arm2 τ 网格**（`sz_l3` 第二次点火挪 80s——旧 35s 会打在 τ=15/20s 未武装完的资源上；120s 试验，τ∈{15,20}s×U∈{1e-4,1.5e-4}）：①外源结论在 τ=15/20s 同样成立（p2 全是 1–4s 瞬态回平台，自由运行周期不存在→周期 vs τ 标度无定义=结论而非缺失）；②**再武装不完全**——p2 峰仅为 p1 的 26–58%（单 τ 模型预测 80s≈5τ 时 d 恢复 99%+ 应≈全幅）→ 资源外存在**更慢第二恢复分量或状态依赖兴奋性**（下态 MBON 0.2–5 Hz 深于自发基线 ~43 Hz=活动性压制持续）；τ 方向定性符合（τ=20 弱于 τ=15）；③平台深度对 U·τ 剂量单调（1.92–2.24 µV，唯一干净模型确认）；④τ=20/U=1e-4 迟发二次爬升（MBON 8→47→13 Hz，尝试再爬但无法闩锁）。诚实边界：网格单种子、第二恢复分量底物未定位、再武装曲线（p2 延迟扫描）为直接后续 | bci/sleep4 |
| **再武装恢复曲线 R(t)** | ✅ | ㉓**sleep5**（`bci/sleep5`）：固定循环剂量（U=1.5e-4/τ=10s/种子 616），第二次点火延迟 D 扫 {10,20,35,50,80}s（35s 复用 sleep3 trace，p1 逐位同 8.35 µV）。**R_mbon：0.044→0.211→0.414→0.429→0.411**（塌缩后 2/12/27/42/72s；R_scalp 同形但短延迟被平台地板 2.3 µV 抬高，D=10 真实瞬态≈0）。三段结构：①快分量按模型 τ 恢复（STD 资源在工作）；②**恢复在 ~42–45% 封顶**——双指数拟合退化为 45% 按 τ=10s + 55% 在 80s 尺度不恢复（τ2→∞），**不是第二慢 τ 而是封顶**；③封顶=点火后平台态的响应上限：p1 从暗态（MBON 基线 43 Hz）出发、p2 从被压制平台态（3–5 Hz）出发，同一 d 下级联只达暗态强度的 ~42–45%。**总结论（sleep2–5 闭环）：真迟滞双稳/活性维持下态**——上态=外部点火的复归吸引子（~400 Hz），被自身放电经慢 STD 2–8s 耗竭熄灭；下态=被压制态稳定 ≥80s、资源部分恢复但响应封顶、噪声无法再闩锁；两态切换全由外部事件驱动（上→下自发，下→上需点火）——本系统是**发作样迟滞对，非内源振荡器**。诚实边界：单种子；封顶底物未定位；d dump+速率联测可分离「响应封顶 vs 资源恢复」 | bci/sleep5 |
| **逐室误差架构（全局 vs 小室门）** | ✅ | ㉔**comp1**（`bci/comp1`）：落地 `--plastic-comp-reward`——**小室 = 向同一 DAN 型反馈（MBMD GABA 行）的 MBON 集合**，奖励与门监视只看该型细胞、mod 变逐边向量小室外全零。机制全链路：`k_plast_adv` 标量→`const float*` 逐边向量（标量路径逐边算术不变=全局门位级不变）、CPU 孪生广播同式、kernel 键随源码自动更新。试点小室 PAM07（14 DAN 细胞←14 MBON，13.72% 可塑边；门增益按池响应重标 0.02→0.16 使 mod 动态范围匹配）。结果（2 臂 × blocked/control × 2 会话，dangate2 协议/标定/种子）：①**全局臂逐会话复现 dangate2**（阻断 1.41×、误差压制 33%，r0/r1 近位级——向量核重构的直接验证）；②**架构限制生效**：comp 臂 p2 边生长封顶 12.1%（≤13.7% 小室上限，两会话一致；全局臂 75–89% 稠密铺开）；③**机制解离：阻断是聚合误差现象**——小室臂阻断 1.00×/压制 3%，p1→p2 mod 积分坍缩（1972→150 ms）在两条件相同且从 p2 第 0 试即现（复合气味聚合 MBON 驱动的即时压制，与学习无关），A 预训练的增量压制（阻断本体）在小室私有 V（14 MBON）下趋零——**聚合误差产生阻断、小室私有误差支持独立联结但不支持阻断**，把「阻断是全局还是小室性」之争变成可测的实现层命题。诚实边界：门增益跨池重标定；w_B 读出被全池子型均值稀释；双小室双奖励独立联结检验为直接后续 | bci/comp1, src/ffbm/{gpu,simulation}.py |
| **扇入密度：APL 否决 + KC→MBON 阈值不敏感** | ✅ | ㉕**fanin1**（`bci/fanin1`）：①**队列原命题否决**——APL→KC 在 w≥5 已覆盖 100% KC（4,104 对/4,064），弱尾 +13% 零新增覆盖；PN→KC 同（93%，+13%）；②**装配层发现**：KC→MBON 弱尾 +27,714 对（+83%），但连接组 raw id（KC 1xxxxx 族）与电路 CEN 表合成 id（10062xxx 族）是**两个 id 空间**——弱行须经 `_rmap` 重映射入表（首轮未映射 KeyError 已修；`--kcm-fanin` 机制保留：重映射+去重+盐化键）；可映射弱行 w≥1 +27,286 → 60,684 边，弱尾与 CEN 成员边界高度重合（w≥5 截断≈建模群体边界）；③**功能答案：阈值不敏感**——dangate2 标定协议 4 臂扫描（base 33,398/f2 50,779/f1 60,684/f1r lr 重标对照），固定 lr 下池 +82% 学习曲线仅慢 ~8%（w_A 0.273→0.252）、RW 递减与稳定性不动、无 latch——**强边核心（w≥5）承载全部回路功能，扇入密度不是学习可见性杠杆**，组装截断被验证。诚实边界：单会话单种子；f1r 缩放用池大小比近似；DAN→KC/KC→DAN 的 +123k/+136k 弱尾在被压制非活性腿上，激活突触级 US 递送才是其战场（方向） | bci/fanin1 |
| **双小室双奖励：独立联结 + 价值表达的状态依赖** | ✅ | ㉖**comp2**（`bci/comp2`）：`--plastic-comp-reward` 推广到 `TYPE:GAIN` 列表（逐池门增益：PAM07 14 细胞 4582 边 gain 0.16 / PPL103 2 细胞 7166 边 gain 0.02——两小室门控边完全不相交），逐室私有监视器（各自 r0 冻结+日志），mod=逐室向量之和；dan trace 改记逐边最大值（向量求和会随门控边数无意义缩放）。协议：A+@PAM07 ×3 → B+@PPL103 ×4，C 预训练对照，comp/glob 双门。结果：①**独立联结成立（两门皆是）**——B 学习比（训练/对照）comp 1.00、glob 0.99，序列式 A→B 训练零串扰；②**过阻断预测被机制否定（设计层发现）**：MBMD 价值反馈是**尖峰驱动、状态依赖**的——A 的 V 只在 A 在场时表达（p2 A 缺席 → A 的 MBON 回基线 → 无压制，两臂 mod_ms 逐位一致）；**过阻断需要 A/B 共现 = comp1 已测的阻断范式**；③**代价收益并行**：私门联结弱（dw_A 0.004 vs 0.18、dw_B 0.022 vs 0.44）但互不干扰、位置特异。**电路层判据：逐室架构的价值在刺激共现范式，序列范式聚合门天然无串扰**。诚实边界：PPL103 仅 2 细胞（泊松噪声入监视器）；单种子；comp 联结幅度小；附带修复 meta.json 被部分续跑覆盖的问题（改合并式写入） | bci/comp2 |
| **再武装封顶底物：d(t) vs R(t) 解耦** | ✅ | ㉗**sleep6**（`bci/sleep6`）：5 个单脉冲 trial（sleep5 剂量/种子）在 sleep5 各延迟点到达时刻截断，trial 末态逐边 d（std-debug dump）= 该时刻资源状态（确定性前缀=同一动力学）。**状态封顶实锤**：80 s 时曾耗竭边（占 CEN_C 全池 84.7%）d 恢复 **0.976**，R_mbon 仍封 0.41——**封顶=平台态上限，非资源**（sleep5 解释获定量确认）；d 平衡值 0.975–0.976 可解析为平台期残余放电的动态平衡（d_eq≈0.976 ⇒ 曾耗竭边等效均率 ~16 Hz，资源赤字仅 2.4%，对 55% 响应损失贡献可忽略）；d(t) 与 R(t) 35 s 后完全分叉=「响应能力」是网络态变量。**sleep2–6 全链闭环**：慢 STD 资源只承担「熄灭」一半（自耗竭熄灭上态），「维持压制」由平台态本身承担（资源近满仍封顶）——迟滞双稳两态由不同变量承载：上态=资源+复归吸引子，下态=网络态压制。诚实边界：单种子；d 为全池统计；网络态压制的分子底物未定位（逐群体速率+电流分解=下一问） | bci/sleep6 |
| **刺激：真实图像输入** | ✅ | 通用视觉输入模块：`visual_inputs.json` 目录 + 灰度视频→小网膜采样（模糊/归一化/每眼整幅眼平面映射）+ 页面同步双视图 | docs/STIMULUS.md §5 |
| **刺激：化学感觉（嗅觉 + 味觉）** | ✅ 嗅觉验证通过 / 味觉外周通过 | 新区域 `olfactory`/`gustatory`（ORN 2,639 / GRN 1,428，连边 ORN_C 57k 对→触角叶、GRN_C 8.4k 对→SEZ）；`--chem-input` 脉冲协议；ORN/GRN 转导级联（共享视觉动力学）；标定发现：中央脑默认增益是点火吸引子（82 Hz 自持），chem 工作点 CEN_C→0.002 后触角叶特异响应可衰减；**嗅觉头皮签名 ×1.4–1.65**；味觉因 GRN→VNC 大头不投影，签名弱（×1.04，800 pA 阴性）| exp019, docs/SENSORY_INPUTS.md |
| **刺激：本体感觉 + 运动意图解码** | ✅ 完成（v1+v2） | 新区域 `proprioception`（PRO 1,454，rootSide L/R 分组；PRO_V→VNC 47.9k 对、PRO_C→中央 4.8k 对）；MI-v1：指令窗可解码（99–100%，0.3% 背景水平、p=0.002，纯 PRO_C 末梢偶极子），延迟窗无保持（首版带通 78–89% 是 filtfilt 长尾伪影）；**MI-v2：DN 通路探针**（DN 输入 392 万突触 96% 已在电路：cb_intrinsic/LAL 61%、DN→DN 13%、上升 AN 13%；PRO 直连仅 0.27%）→ 想象窗内部驱动（LAL* 路由 1200 pA / DN 直驱 800 pA 对照，各 12 试次）：**想象窗全程可解码**（路由 +0.001~0.002 µV p≤0.013；直驱 −0.005~−0.008 µV p≤0.002，谷氨酸能极性反转），无点火 | exp020, docs/SENSORY_INPUTS.md §4 |
| **刺激：其他模态（听觉）** | ⏸ 规划 | 听觉 JO+AMMC 数据在、社区空白 | docs/SENSORY_INPUTS.md §5 |
| 64/128/241 导精确波形重导出 | ⏸ 待定稿 | GPU+缓存后每配置约 3-8 min（首次核构建为主；EGI241 最慢），命令见 USAGE §6 | — |

## 3. 超参数：写在哪里、怎么改

| 位置 | 内容 | 修改方式 |
|---|---|---|
| `src/ffbm/params.py`（SECTIONS 注册表） | 全部模型超参：LIF、突触、光转导、MT、工作点、VPN/中央脑、人脑四球壳、**刺激协议**（时段表/对比度/漂移速度/种子等） | 直接编辑；`python -m ffbm.params` 重新生成 docs/PARAMS.md（有测试保证文档不过期） |
| `params.cal()` | 运行时 CAL 字典（键名即覆盖入口，按次序解析） | 程序化覆盖：`cal = dict(fp.CAL); cal["I_R_BASE"]=...` |
| `viz/data/elec_configs.json` | 电极配置目录（45/64/128/EGI241 + 自定义） | `viz/make_elec_configs.py` 重新生成，或手工追加 `kind:"custom"` 条目 |
| `viz/data/elec_layout_1010.json` | 当前导出用的 45 导布局 | `viz/make_elec_layout.py`（消费标定面板导出的 JSON） |
| 浏览器 localStorage | 页面三类标定的工作状态（calib1020 / viewDirs / headFormCalib_v3）+ 电极编辑草稿 | 页面滑块/手柄直接调；「复制JSON」可导出 |
| `viz/export_data.py` CLI | `--elec-layout`、`--regions` | 命令行 |

状态标签含义（PARAMS.md 每行第三列）：dataset=数据集直接给出｜
literature=文献值｜calibrated=对实测波形拟合｜phenomenol.=现象学构造｜
assumed=工作假设｜numerical=算法选择（已验证）｜chosen=设计选择｜
deferred=开放项。

## 4. 文档索引

| 文档 | 内容 |
|---|---|
| HANDOFF.md | 交接：环境恢复、数据事实、API 参考、bug 史、领域调研 |
| docs/TECHNICAL.md | 方法学：连接组→电路→仿真→前向核→背景 EEG 的全部数学 |
| docs/PARAMS.md | 超参数注册表（自动生成，勿手编） |
| docs/STIMULUS.md | 刺激协议：实现、参数、真实图像可行性 |
| docs/USAGE.md | 本手册：页面操作、标定工作流、导出流程 |
| docs/data.md | 数据文件说明 |
| viz/ELEC_CONFIGS.md | 电极配置文件格式 + 四套配置说明 |

## 5. 已知限制

- 刺激仅视觉：合成协议（1-D 纹理线扫描 + 全场闪烁）与真实视频输入
  均已实现（visual_inputs.json 目录）；真实视频作为正式"实验范式"
  使用前建议核对 docs/STIMULUS.md 的映射限制说明（归一化统计口径等）。
- R7/R8（色觉）未装配；色觉输入需先补小网膜 R7/R8 电路。
- 颈部 VNC 只参与动力学、不投影到头皮（位于放大头球之外，物理上
  正确）。
- 64/128/241 导目前为插值预览波形；精确导出待定稿后执行（§2 末行）。
- EGI 256 是厂商测地几何（E 编号无解剖含义），与规则推导的 10-x 帽
  不完全重合（预期行为）。

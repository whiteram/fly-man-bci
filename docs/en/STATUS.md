# Project Progress Overview (STATUS)

*(English translation — authoritative version: [../../docs/STATUS.md](../../docs/STATUS.md))*

Updated: 2026-09. Builds on HANDOFF.md (handoff/environment/data) and docs/TECHNICAL.md
(methodology details); this document answers "where are we right now".

---

## 1. Current status in one sentence

Population-spiking simulation of the fruit fly visual system (MaleCNS v1.0
connectome, optional per-region assembly) under natural visual stimuli is now
working end to end, and is projected to scalp EEG via a four-shell human-head
forward kernel, with full 3D visualization, an interactive calibration system,
and several electrode configurations; stimuli support both synthetic protocols
and **real video input** (grayscale → ommatidia-sampling mapping,
`visual_inputs.json` catalog + a synchronized dual view on the page); the
extension path for other modalities is in docs/STIMULUS.md §6.

## 2. Completed simulation stages

| Stage | Status | Output | Code/experiments |
|---|---|---|---|
| Data pipeline (connectome download/subnetwork/annotations) | ✅ | visual subnetwork + annotations + neurotransmitters + coordinates | scripts/, docs/data.md |
| ERG forward (phototransduction → R → lamina → eye-surface potential) | ✅ | benchmarked against measured ERG waveforms | exp001–002 |
| ON/OFF asymmetry, moving gratings | ✅ | direction-response baseline | exp003–004 |
| T4/T5 direction selectivity made mechanistic | ✅ | Hex-axis DS circuit (T4/T5 preferred −135°) | exp005–006, exp013 |
| In-head "fly EEG" + conductor model | ✅ | sealed two-sphere kernel SealedHeadPairField | exp007–008 |
| Lamina polarity/current-pattern corrections | ✅ | RL/LM current basis + mechanistic LMCs | exp009, exp013 |
| Human-scale thought experiment (×400 fit into the four shells) | ✅ | FourSpherePairField + adaptive scale | exp010 |
| Stimulus detection theory (signal vs background EEG) | ✅ | T7 d′≈2 (about 46 trials) | exp011 |
| Return-current constraint/conservation checks | ✅ | — | exp012 |
| Bilateral optic lobes + VPN→central brain + full CNS assembly | ✅ | optional region assembly (regions toggles); full-CNS 150,601-neuron simulation (all Traced∩has-soma cells of the three superclass families; coverage criteria in HANDOFF §4.3) | exp014–017, regions.py |
| Scalp electrode-array expansion | ✅ | four configurations: 45/64/128/EGI241 (elec_configs.json) | make_elec_configs.py |
| **Compute acceleration: numba + GPU + staged caches** | ✅ | numba kernels 2.38×; `--gpu` loop 47 min→156 s (bit-identical trajectories); circuit/kernel caches (switching electrode configs rebuilds only the kernels) | docs/ACCELERATION_PLAN.md P2/P3/P5 |
| Interactive calibration system | ✅ | 10-20 handle calibration (ni/th/yaw/roll), view-direction calibration, head-form calibration | viz/index.html |
| Visualization page | ✅ | electrode-name labels, heatmap, epoch color bar, configuration switching, preview waveforms | viz/index.html |
| **Scalp-EEG EDA (distributions / channel differences / stimulus decodability)** | ✅ | clean signal band-passed 0.5-6 Hz decoding CV R2=0.84, cascade lags 23/17 ms, spatial participation rank ~2; band distribution delta-dominated (0.1-4 Hz holds 72%, no alpha peak, AC amplitude ~130x below human background); premise = fly replaces the brain (sensor noise only): 0.1-1 Hz + PC spatial filtering reaches single-clip SNR~1.2, ~3 clip-averages to SNR 2 | exp018 |
| **BCI paradigm-acquisition branch (SSVEP-Benchmark)** | ✅ | aligned to Wang 2016 (40 classes, 8-15.8 Hz); multi-seed 40 classes x 5 repeats: CCA top-1 72% clean / 83% with scaled hypothetical equipment noise (chance 2.5%, same range as human benchmark); averaging does NOT help -- the limit is 0.2 Hz class spacing, not noise; SSVEP-band responses ~27 dB in-band SNR -- low-frequency dominance was the stimulus, not the model | bci/ssvep_benchmark |
| **BCI paradigms: motor imagery / olfactory / auditory** | ✅ | `motor_imagery` (2-class, reuses exp020 MI-v2): imagery-window spatial LOO 100% (p=0.003); `olfactory` (3-odor identity, exp019 circuit, ORN-group-size-compensated amplitudes): 3-class 89% (p=0.0005, DA1/DM2/VA1v separable in spatial pattern); `auditory` (new region `audition`: JO-A 115 -> JOA_C 1,578 pairs / 24,392 syn -> central brain; song-envelope modeling): 4-class 54% (p=0.007) / side 75% / mode 75% -- total rms identical across classes, separability entirely in the spatial pattern; `cvep` (BETA-aligned, 127-bit m-sequence with 40 cyclic shifts): **40-class template matching 100% (p=0.0005, chance 2.5%)**; `looming` 3-class 72% (motion-vs-static 100%, expansion-direction 58% = chance -- direction selectivity not scalp-visible); `direction` 4-class 46% (p=0.022, temporal template; spatial rms 42% n.s.); `mrcp` (LAL staircase ramp + PRO execution): **pre-movement ramp L/R 100% (p=0.0035)**, intent decodable before execution; also fixed a chem_fn same-pop multi-channel overwrite bug (now sums; equivalent for old protocols); `seizure` (`no_cen_damp` exits the safe working point; the latch IS the seizure): seizure-vs-healthy 100% (p=0.0025), focus lateralization 100% (p=0.026), plus a finding of **spontaneous ictogenesis** (at default gain pure noise self-ignites in ~0.55 s, before the focal drive); `touch` (new region `touch`: TC 2,558 -> CEN 17k syn): touch-side 2-class 100% (p=0.0035); `gustatory` 3-organ identity 39% n.s. -- honest negative, GRN_C at 8.4k pairs is structurally invisible (exp019 lesson reproduced); haltere builder vetoed by the data (201/205 already inside PRO); **fixed pipeline `_indices` dense-lookup OOM** (37 GiB at the TC 5e9 band; now searchsorted, saves GBs in every run); round-4: `p300` (peripheral-adaptation oddball) target deviance 6x (AUC 1.00, p=2e-4; honest framing = adaptation, not cognition); `cnv` (S1->LAL ramp->S2 two-stimulus) anticipation L/R 100% (p=0.0035); `auddev` envelope-rate deviance negative (cascade adapts to mean level, rate-blind -- mechanism direction); seizure **gain sweep**: 0.002-0.0035 all healthy, 0.004 self-ignites -- hard bifurcation between 0.0035 and 0.004, no controllable intermediate gain, controllability = laterality not onset; `odormix`: mixture nonlinearity AB/predicted = 0.80 (20% sublinear suppression) + identity/order decodes both 100% (enabled by the chem summing fix); `dose` 3 concentration levels 100% (Hill-type saturating curve); `hybrid` SSVEP x imagery: imagery stream 100% but the visual frequency line is masked ~500x by central OU background (CCA/PSD/spatial filtering all fail -- extreme dual-task interference, honest record) ;round-6: `stereo` stereo-olfaction (ORN rootSide side groups) 2-class 100% (p=0.0035); `visualfield` (new eye_map half_split monocular geometry) 2-class 100% (p=0.0035) -- spatial pattern is the most robust readout axis; `arousal` OU-noise state axis **negative + mechanism finding**: spontaneous activity is bias-current driven (noise x0.05 moves floor only -0.8%), a true state axis needs baseline-current modulation = mechanism direction ;tier-3 mechanism 1: **short-term synaptic depression** (Tsodyks-Markram resource gate; spec/`--std-gates` opt-in; CPU+GPU paths; depletion at release through the delay ring; STD-off bitwise unchanged); `mmdev` identity-oddball train: repetition suppression A5/A1=0.02 (dominant = cascade adaptation), **deviance ratio devB/A5 ~ 3**, STD marginal contribution +16% ;mechanism 2: **working-point state axis** (`--base-scale` bias-current scaling) -- `gainstate` shows floor, stimulus gain and response reliability all move with the working point (odor response 2.4x, low state enters stochastic near-threshold regime with 140x trial variance) -- state and gain share one knob (the gain-modulation definition, inseparable) ;mechanism 3: **DAN-gated KC->MBON plasticity** (`--plastic-mb` splits 33,398 pairs into a plastic group + `--plastic-window/lr` three-factor depression rule, CPU+GPU, plast-off bitwise unchanged) -- `condit` driven conditioning: **learning signature +105% (p=0.026, scalp-visible)**, direction consistent with MBON disinhibition; side finding: the PN->KC pathway is suppressed by the chem working point (odor identity cannot reach KC; true odor conditioning = AL-gain independence, mechanism direction); diagnosed and fixed a GPU trial.run mod_fn omission | bci/*, exp021, docs/SENSORY_INPUTS.md §4-5 |
| **Cross-trial memory + acquisition-extinction curves** | ✅ | mechanism 4: KCM plastic weights now **persist across trials** (`--plastic-state-in/out` state chaining; edge-identity md5 checksum -- the synapse's internal lexsort order is a deterministic function of the table content, mismatches are rejected; eligibility zeroed between trials = ISI >> 400 ms) + **slow homeostatic recovery** `--plastic-tau-w` (w+=(1-w)dt/tau, gate-independent; 0 = frozen, bitwise unchanged; CPU + GPU `k_plast_adv` rec parameter); `condit2` acquisition-extinction paradigm (5-s trials, one probe each: 10 rewarded acquisition -> 8 reward-omitted extinction -> 8 never-rewarded control, 2 seeds each, 52 trials): **acquisition slope +0.0053 uV/trial (perm-p=0.0002, single-trial jump then plateau, w -> 0.15 equilibrium), extinction -0.0142 (p=0.0002, probe decays -37%, w 0.28 -> 0.78), control flat (p=0.30 n.s., w stays 1.00)**, early-vs-late paired +0.033 vs ctrl +0.001 uV -- the project's first **trial-by-trial learning curve**; honest limits: the reinforcement gate is still a time-window proxy, recovery is homeostatic not anti-Hebbian; also regenerated docs/PARAMS.md (5 region base-current params left stale by earlier rounds) | bci/condit2, src/ffbm/{simulation,gpu,pipeline}.py, viz/export_data.py |
| **AL gain independence + attention gain modulation + noise-robustness table** | ✅ mechanisms 5-6 | (1) `--al-gain`: splits AL->KC (19,511 PN->Kenyon pairs) out of CEN_C into independent-gain group ALK, escaping the chem working point's 500x damping -- **a real odor (ORN->PN->KC) drives Kenyon cells for the first time** (elig 31 vs control 0.84); no clean stability window (sustained component scales continuously with gain; the control itself has a +0.44uV plateau), working point g=0.01; `condit3` true-odor conditioning (lr recalibrated 5e-5, w -> 0.94 / driven subset 0.34): **honest negative** -- train/probe scalp responses identical learn vs nolr (full-gain ORN/PN periphery dominates; the readout leg KCM at 0.002 is 500x weaker); output-leg frontier (--gain-scale KCM x50/x250): x50 safe but +5% marginal, x250 visible (train x0.5) but nolr latches -- **plasticity = automatic latch protection** (the learn arm self-depresses back to stability), the expression-stability trade-off sits exactly at the ignition boundary; (2) `--gain-scale GROUP=f` runtime pathway-gain modulation (g_unit is a runtime scalar, no cache rebuild): `attend` attention experiment (OLR_V x4/x16): **cannot recover the masked SSVEP line** (SNR 0.72 -> 1.17, ~25 dB short), imagery stream zero cost (8/8 all arms), **hybrid masking attribution corrected** (nwp control arm: working point x1.0, dual task x1.4, the ~300x comes from central-region inclusion itself -- hyb_n's missing chem flag had no measured effect; hybrid README carries a correction note); (3) `noise_robustness` analysis-only table (15 paradigms x 5 signal-level white-noise levels + uniform spatial-rms LOO): spatial-signature paradigms hold 100% at r=10 (window integration beats white noise), cvep collapses immediately (its information is temporal code) | bci/condit3, bci/attend, bci/noise_robustness, viz/export_data.py |
| **Blocking test + motivation-gated learning** | ✅ | (7) **Kamin blocking test** (`bci/blocking`, KC subtypes as synthetic odors A=KCg-m/B=KCab-m/C=KCab-s, state-chained sessions with frozen weights, 40 trials): **NO BLOCKING** -- A+ pre-training has zero effect on B's learning during AB+ (final w_B floors at 0.0200 in both conditions, diff +0.0000, perm-p=1.0; scalp B-curve diff -0.003uV, p=0.66) -- the rule-class boundary of the pure three-factor rule (elig x mod, no error term) confirmed; a reward-prediction-error gate (mod prop. delivered minus predicted) is the flagged mechanism direction; side finding: **background leakage** from spontaneous KC firing during open gates (untrained subtype w -> 0.94 over 10 trials); state files now carry per-edge `pre/pre_type` (w decomposable by subtype); (8) **motivation gating** (`--plastic-mod-scale` gate-strength axis + `bci/motivate`, 20-trial single-shot dose-response): **perfectly monotone** -- mod 0.125 to 1.0: w 0.855 -> 0.286, probe delta +0.037 -> +0.126uV (Spearman rho = -/+1.0, p=0.08 = the theoretical floor of a 4-arm rank test; within-arm sd <= 0.003, near-deterministic) -- the motivation/reward-strength axis enters learning depth linearly, isomorphic to hunger-enhanced conditioning; also (9) **APL feedback-loop surgery** (`--mb-mod-gain` splits the 8,304-pair KC<->APL GABAergic negative-feedback loop to an independent gain; DPM's dopaminergic positive loop excluded by design -- measured 780uV nolr latch when included): four-point characterization, **honest negative** -- traces are pure DC fixed points (not oscillation); additive APL feedback shifts but does not remove them (KCM x250 latch deepens to 131-771uV; KCM x50 + g0.05 shows a +73% train signature but both arms sit on elevated plateaus = confounded); the three-factor plasticity remains the only effective stabilizer; a shunting-conductance implementation is flagged as new-project level | bci/blocking, bci/motivate, viz/export_data.py |
| **Short/long memory dissociation + thermal/humidity modality** | ✅ | (10) **dual-tau compartments** (`--plastic-dual-tauw 30000,300000`: KCM split by presynaptic KC subtype into KCMg 14,298 pairs / KCMab 19,100 pairs with independent recovery; dedicated salt +plmb2 against topology cache collision; state files carry per-group w_<grp> with legacy single-group compat): `dualmem` (two probes per trial, gamma/ab windows, 4 sessions x 16 trials) -- mechanism level after extinction **gamma w=0.79 (nearly forgotten) vs alpha/beta w=0.15 (5.3x retention)** (bit-identical, deterministic); scalp level gamma decay -43% vs ab -19%, all 4 sessions in the predicted direction, slope-diff perm-p 0.121 = **the exact floor of this test** (2/16) -- the bottleneck is session count, not effect; (11) **new modality region thermal** (exp023: TH 25 TRN + HY 66 HRN dual pops, TH_C 1,257 pairs/45k syn + HY_C 2,524/46k -> CEN; 3-s slow ramps): `bci/thermal` **temperature warm/cool spatial LOO 92% (p=0.017, total rms flat -- another spatial-pattern-axis result)**; humidity dry/moist 58% negative (structural hypothesis: dry/moist central targets overlap more) | bci/dualmem, bci/thermal, exp023_thermal, viz/export_data.py |
| **Prediction-error gate + model-physiology validation table + sleep probes** | ✅ | (12) **error-gate blocking** (`bci/blocking2`, experiment-layer Rescorla-Wagner gate: error = R - sum V, alpha 0.3, V persisted per session, injected via --plastic-mod-scale with the synaptic rule untouched, 40 trials): **blocking emerges** -- w_B blocked 0.5997 vs control 0.1255 (control learns 4.8x), textbook V trajectories (V_A -> 0.88 -> AB+ error 0.12); combined with bci/blocking: **blocking is a property of the gate's computation class, not of the synapse** (window gate: no blocking; error gate: blocking; same rule); (13) **validation table** (docs/VALIDATION.md): KC spontaneous rate **0 Hz vs literature ~0.004 Hz -- match** (steady-state elig probe; also corrects the background-leak interpretation = network-driven, not autonomous noise), KC evoked rates 1-2 orders high (mechanistic account = the same sparsening deficit as the APL arc), odor onset latency 126 ms order-consistent, ERG per exp001; (14) **sleep bistability probes** (3 x 60 s: cen_gain 0.004 + STD gate = down all the way, zero transitions; sz_l up-start + U 0.02/0.1 = sustained 10.5uV plateau INDEPENDENT of U) -- bistability not achieved; the up-state's maintenance path does not run through CEN_C recurrence (candidate: noise-driven population spiking), flagged as a mechanism question; also: single-group salt +plmb1 (the pre-plmb2 dual pilot had polluted +plmb), --mb-mod-tau (tau 3 ms still latches; APL variants exhausted) | bci/blocking2, docs/VALIDATION.md, viz/export_data.py |
| **Up-state attribution + PN amplification (latent-sign fix)** | ✅ | (15) **up-state maintainer identified** (`bci/sleep2`, follow-up to mechanism 14): per-pathway zeroing sweep (--gain-scale, 6x20 s) OVERTURNS the earlier inference -- the maintainer IS the CEN_C recurrence at full 0.004 gain (plateau 10.35 -> 1.39 uV collapse, all classes <1 Hz when zeroed; OLR/visual/ORN feedforward zeroing leaves it intact); the mechanism-14 U-independence reinterpreted = the STD gates as parameterized never bit the maintaining current -> viable sleep-bistability route = slow variables (tau_rec >= 5 s resource gates or adaptive i_base). (16) **PN weak-signal amplification** (`bci/pnrate`, VALIDATION #5 closed): --pop-rate + dose sweep, PN/ORN evoked ratio **15.1x at threshold -> 3.2x at 4x intensity** (expansive nonlinearity, qualitative MATCH to Bhandawat 2007; from 30:1 convergence + LIF thresholding, not dendritic mechanisms). Byproduct: **extra-edge latent-sign gap** (sign column never reached the pools -- central synapses ran all-excitatory vs the exp017 README claim); MBMD opts in via use_sign; global enablement = separate project | bci/sleep2, bci/pnrate, docs/VALIDATION.md, src/ffbm/pipeline.py |
| **Circuit-level error gate (mechanism 15)** | 🟡 | (17) **dangate** (`bci/dangate`): the Rescorla-Wagner error moved INTO the circuit -- potentiated KC->MBON readout (negative lr + w0; k_plast_adv dual branch + CPU mirror + 5 tests) -> MBON->DAN 349 GABA rows split as MBMD (absolute gain + use_sign) -> reward and V act on the SAME DAN_err compartment (154 cells) -> mod=clip((r-r0)*g,0,1) read from the previous step's spikes (causal). Three calibration finds: KCM g_unit silently clobbered by the chem working point (fixed); mixed signs are net-EXCITATORY in conductance mode (driving force 55 vs 25 mV; 244 excitatory rows dropped = minimal negative-feedback architecture); MBON binary operating point (kcm 0.015 + w0 0.03 straddle the switch). Full (2x2x10 trials, lr -2e-7): **circuit-level blocking emerges but weak** -- w_B blocked 0.267 vs control 0.296 (1.11x), error suppression 12%, early-phase separation 55%; **compartment-specific error coding**: control p1 (C+) error flat at 1950 (V_C never enters the compartment). With blocking/blocking2: window gate 0x -> RW gate 4.8x -> circuit gate 1.11x -- blocking weakens but survives the implementation climb; the weakness is the MBON binary switch (41% gate floor); strengthening route = MBON hyperpolarizing bias / lower reward amplitude (new project) | bci/dangate, viz/export_data.py, src/ffbm/{simulation,gpu,pipeline}.py |
| **MBON headroom restoration (mechanism 15 strengthened)** | ✅ | (18) **dangate2** (`bci/dangate2`): diagnosed the dangate bottleneck as **suppression-transfer saturation** -- the untrained baseline MBON firing (~43 Hz) already maxes MBMD, blocked/control DAN traces nearly identical (8.5/8.8 Hz), the gate saturates BEFORE V can differentiate. Fix = `--mbon-bias` (MBON* tonic hyperpolarizing chem channel, 96 cells, runtime amplitude never in a cache key, ~20 lines). Static transfer-chain calibration (scan_summary.json): bias -300 pA moves the untrained point below the w-knee -- r contrast **51/10 Hz ~ 5x (vs 1.05x at bias 0)**, gate gain retuned 0.05 -> 0.02; flat below 150 pA (excitatory drive dwarfs the bias), -600 pushes the whole curve right and loses the trained end. Full (2x2x10): **blocking ratio 1.11x -> 1.41x, error suppression 12% -> 33%, early-phase separation 1.8x**, and the first **in-circuit RW decrement** (blocked p1 mod 1808 -> 792, control p2 1778 -> 646 -- error shrinks trial-by-trial as V grows, no experiment-layer involvement; blocked p2 gate settles at mod ~ 0.32 = the w growth/recovery equilibrium). Honest notes: control p1 C never enters the compartment (inherited from (17); control = novel AB+, Kamin design intact); the remaining gap to the experiment-layer 4.8x = single-compartment error sum vs the real per-compartment architecture + the recovery term setting the equilibrium | bci/dangate2, viz/export_data.py |
| **Slow-variable sleep bistability + STD recovery-kernel bug fix** | ✅ | (19) **sleep3** (`bci/sleep3`): per the sleep2 attribution, a slow STD hung on CEN_C alone (tau_rec seconds-to-tens). The U sweep 0.001-0.2 came back bit-identical to control at every dose -> deep-dive found a **latent computation bug**: `k_std_rec` (GPU) and its CPU twin recover with `d += (1-d)*rec` where rec=exp(-dt/tau)~1 = **instant refill every step; STD was an inert gate at every (U, tau)**. Fixed to `*(1-rec)` (per-step fraction dt/tau, timescale tau); per-spike probe verifies d 0.1->0.01->0.001 with 0.0023%/step recovery = dt/tau exactly; 55/55 tests pass. **This bug IS the mechanism-14 mystery 'the STD gate never bit the maintaining current'** (not a dynamical cause). Historical impact: mmdev's std_on/off contrast and every pre-fix --std-gates result need re-audit. Post-fix dose map (60 s two-pulse hysteresis design sz_l2, level 10-30 s): at tau=5 s, U<=2e-4 gives a **family of depleted up-states** (graded fixed points 4.66->3.25 uV), U>=3e-4 collapses (dwell 3.5->2 s), 2.5e-4 is the boundary (collapse + noise-driven re-flares in the down state); **U=1.5e-4, tau=10 s yields a full slow oscillation cycle**: up ~5 s (MBON 225->80) -> collapse -> down ~28 s (~3tau re-arm) -> second ignition re-latches -> collapses again. sleep2's attribution + the fixed slow STD = the minimal slow negative feedback: the up-state's own firing depletes its maintaining current (d_eq = 1/(1+U*r*tau); sustaining threshold localized to 0.5-1x 0.004 by a gain bisection). Honest notes: re-ignition needs the external pulse (no spontaneous flips observed), the down state carries a ~2.3 uV residual, the gate is population-granular | bci/sleep3, src/ffbm/{gpu,simulation}.py, viz/export_data.py |
| **mmdev re-audit (closing the STD-bug fallout)** | ✅ | (20) **mmdev rerun** (`bci/mmdev`): **retracts the old 'STD marginal contribution +16%'** -- under the bug the two conditions differed only by f32 last-bit chaos + seed sampling. Post-fix: std_off reproduces the old numbers bitwise (2.74), std_on at the original U=0.25 crushes the whole ORN->CEN pathway (A1 0.207 -> 0.0006 uV = 300x; repetition suppression 0.02 -> 0.48 = genuine synaptic repetition suppression emerges; deviance 7.83 is denominator-collapse-driven). U dose supplement (0.05/0.1 x 2 seeds): A1 preserved (0.200/0.172) but **the deviant pulse self-depletes** (600 pA = 4x stronger -> deeper within-pulse d_eq -> devB 7x lower -> deviance 0.78/-0.62) -- a new mechanistic characterization: **STD = intensity-dependent gain control** (self-attenuation of strong stimuli, consistent with real ORN->PN gain control) that conflicts with the oddball design assumption 'deviant = strongest pulse'; a matched-intensity deviant design is needed to cleanly separate novelty from intensity. Diagnostic dump: active ORN edges' effective rate only ~3 Hz (consistent with pnrate's low ORN rates) | bci/mmdev |
| **Matched-intensity/response deviance + DC state switch** | ✅ | (21) **mmdev2** (`bci/mmdev2`): executes the clean deviance design the re-audit called for, in two steps. (1) **Matched intensity** (`mmn_match`, DM2@150): the deviant is completely invisible at the scalp (devB -0.0008 +-0.0004 uV, devB/A1 ~ -0.01 in all conditions; the old 2.74 deviance ratio was carried entirely by the 4x intensity). Solo dose calibration (`da1_solo`/`dm2_solo` + `--chem-amp-scale`) finds the **DM2 threshold cliff at 466-492 pA** (466 bimodal 0.027/0.090 = unreliable cliff region; 492 on the stable plateau +0.247 +-0.009 vs DA1@150 +0.197 = **calibrated ratio 1.25**). (2) **Matched response** (`mmn_matchr`, DM2@492): in-oddball devB +0.0095 +-0.0005, ~25x BELOW its solo calibration -> the pre-registered three-way readout lands on **cross-adaptation**: the standard train's adaptation transfers across odor identity through a shared downstream substrate. **Surprise main finding: a DC state switch** -- every oddball net quantity rides on a sustained +0.44 uV platform (mean -1.389 -> -1.825, std 0.10 -> 0.18): any effective single pulse (DA1@150, DM2@466/492) triggers the same full-amplitude **-0.430 +-0.002** step (switch threshold far below the fast-response threshold -- DM2@466 has fast net only +0.059 yet a full step); it saturates with ONE pulse, after which A5/devB (bitwise same at 150 and 492 pA)/recA steps <= 0.004 = floor, **identity- and amplitude-blind**; U=0.05 leaves it untouched, U=0.1 destroys the switch itself (A1 step -0.107 +-0.188 bimodal -- rides the same ORN->CEN pathway as the fast response); **zero recovery over 8 s** (`da1_dc8`, both seeds flat at -1.820 +-0.002, tau > 8 s quasi-permanent flip) -- the dominant slow component of the olfactory scalp signal (3x the A1 fast net). **Conclusion: no MMN** (neither fast increment nor DC step carries identity novelty; mmdev's "identity deviance established" is retracted with it); oddball deviance ratios = intensity artifact + denominator collapse + floor noise. DC-mechanism localization, cross-adaptation substrate, DC decay tau = follow-up directions | bci/mmdev2 |
| **Endogenous test of the slow oscillation + tau>=15 s dwell** | ✅ | (22) **sleep4** (`bci/sleep4`): closes sleep3's honest note (re-ignition leaned on the external pulse). **Arm 1 endogenous test** (`sz_l1` single ignition, 90 s trial, U=1.5e-4/tau=10s, two seeds): the pulse-free [40,90]s window (post-collapse ~8 s + 3tau=30 s re-arm) shows **zero spontaneous re-ignition** (max 2.36 uV, MBON 3-5 Hz vs ctl latch 388 Hz, >=8tau of quiet) -> **the slow oscillation is externally driven, directly demonstrated** (noise alone cannot re-cross the latch threshold). Bonus determinism check: sz_l1_s616 first 60 s bitwise == sz_l2_s616. Trace refinement: at slow-STD doses the down state's scalp signature is a ~2.3 uV plateau (ALPN 38 Hz residual lifts it; MBON 3 Hz = truly down in circuit terms), not the 1.4 uV deep-collapse level seen at high U; the up phase is really a 1-2 s transient (MBON 157 Hz decaying). **Arm 2 tau grid** (`sz_l3` second ignition moved to 80 s -- the old 35 s pulse would hit partially-armed resources at tau=15/20 s; 120 s trial; tau in {15,20} s x U in {1e-4,1.5e-4}): (1) external drive holds at tau=15/20 s (every p2 is a 1-4 s transient back to plateau; no free-running period exists -> period-vs-tau scaling is undefined as a conclusion, not a gap); (2) **re-arm is incomplete** -- p2 peaks reach only 26-58% of p1 (single-tau model predicts d recovered to 99%+ at 80 s ~ 5tau -> p2 ~ full amplitude) -> a **second, much slower recovery component or state-dependent excitability** exists beyond the resource (down-state MBON 0.2-5 Hz is deeper than the spontaneous baseline ~43 Hz = active suppression persists); the tau direction is qualitatively consistent (tau=20 weaker than tau=15); (3) plateau depth is dose-monotonic in U*tau (1.92-2.24 uV -- the one clean model confirmation); (4) tau=20/U=1e-4 shows a delayed secondary climb (MBON 8 -> 46.6 -> 12.7 Hz -- the network still attempts to re-latch from the plateau but cannot cross threshold). Honest bounds: single seed per grid arm; the second recovery component's substrate is unlocalized; a re-arm curve (scanning the p2 delay) is the direct follow-up | bci/sleep4 |
| **Re-arm recovery curve R(t)** | ✅ | (23) **sleep5** (`bci/sleep5`): fixes the cycle dose (U=1.5e-4/tau=10s/seed 616) and scans the second-ignition delay D over {10,20,35,50,80} s (D=35 reuses sleep3's trace -- p1 bitwise identical at 8.35 uV). **R_mbon: 0.044 -> 0.211 -> 0.414 -> 0.429 -> 0.411** (2/12/27/42/72 s post-collapse; R_scalp has the same shape but short delays are lifted by the 2.3 uV plateau floor -- the D=10 true transient is ~0). Three-part structure: (1) the fast component recovers with the model tau (the STD resource does work); (2) **recovery caps at ~42-45%** -- the double-exponential fit degenerates to 45% recovering with tau=10 s + 55% not recovering on an 80 s scale (tau2 -> infinity): **not a second slow tau but a ceiling**; (3) the ceiling is the plateau state's response limit: p1 starts from the dark state (MBON baseline ~43 Hz), p2 from the actively suppressed plateau (3-5 Hz); at the same d the cascade reaches only ~42-45% of dark-state strength. **Overall conclusion (closing sleep2-5): a genuine hysteretic bistability / actively maintained down state** -- the up state is an externally triggered recurrence attractor (~400 Hz) extinguished in 2-8 s by its own firing through slow STD; the down state is a suppressed state stable for >=80 s whose resources partially recover but whose response capacity caps at ~45% and which noise cannot re-latch; both transitions are externally driven (up->down spontaneous, down->up needs ignition) -- this system implements a **seizure-like hysteretic pair, not an endogenous oscillator**. Honest bounds: single seed; the ceiling's substrate is unlocalized; a d-dump + rates co-measurement can separate "response ceiling" from "resource recovery" | bci/sleep5 |
| **Per-compartment error architecture (global vs compartment gate)** | ✅ | (24) **comp1** (`bci/comp1`): lands `--plastic-comp-reward` -- **a compartment = the MBON set feeding back (MBMD GABA rows) to ONE DAN type**; the reward channel and gate monitor see only those cells and mod becomes a per-edge vector, zero outside the compartment. Full mechanism chain: `k_plast_adv` scalar -> `const float*` per-edge vector (the scalar path's per-edge arithmetic is unchanged = global gate bitwise), CPU twin shares the arithmetic via broadcasting, kernel cache key updates with the source. Pilot compartment PAM07 (14 DAN cells <- 14 MBONs, 13.72% of the plastic edges; gate gain retuned per pool 0.02 -> 0.16 to match the mod dynamic range). Results (2 arms x blocked/control x 2 sessions, dangate2 protocol/calibration/seeds): (1) **the global arm reproduces dangate2 session-by-session** (blocking 1.41x, error suppression 33%; r0/r1 near-bitwise -- a direct validation of the vector-kernel refactor); (2) **the architectural restriction works**: comp-arm p2 edge-growth caps at 12.1% (<= the 13.7% compartment bound, both sessions agree; the global arm spreads over 75-89% of edges); (3) **mechanistic dissociation: blocking is an aggregate-error phenomenon** -- the comp arm shows blocking 1.00x / suppression 3%; the p1->p2 mod-integral collapse (1972 -> 150 ms) is identical across conditions and present from the first p2 trial (the compound's aggregate MBON drive suppressing PAM07 instantly, learning-independent); A-pretraining's incremental suppression (the essence of blocking) vanishes under a compartment-private V (14 MBONs) -- **aggregate error yields blocking; compartment-private error supports independent associations but not blocking**, turning the literature debate "is blocking global or compartmental?" into a testable implementation-level question. Honest bounds: the gate gain is retuned per pool; the w_B readout is diluted by the all-pool subtype mean; the two-compartment two-reward independent-association test is the direct follow-up | bci/comp1, src/ffbm/{gpu,simulation}.py |
| **Fan-in density: APL falsified + KC->MBON threshold-insensitive** | ✅ | (25) **fanin1** (`bci/fanin1`): (1) **the queued item is falsified** -- APL->KC already covers 100% of KCs at the w>=5 cut (4,104 pairs / 4,064 KCs), the weak tail adds +13% synapses and zero coverage; PN->KC likewise (93%, +13%); (2) **an assembly-level finding**: the KC->MBON weak tail is +27,714 pairs (+83%), but the connectome's raw ids (KC 1xxxxx family) and the circuit's synthetic CEN ids (10062xxx family) are TWO id spaces -- weak rows must be remapped through `_rmap` to enter the table (the first attempt hit a KeyError at the delay lookup, fixed; the `--kcm-fanin` machinery stays: remap + dedupe + cache-key salt); mappable weak rows give +27,286 -> 60,684 edges at W=1, and the weak tail aligns closely with the CEN-membership boundary (the w>=5 cut essentially IS the modeled-population boundary); (3) **the functional answer: threshold-insensitive** -- a 4-arm scan under the dangate2-calibrated protocol (base 33,398 / f2 50,779 / f1 60,684 / f1r with lr rescaled by the pool ratio): at fixed lr, +82% pool slows the learning curve by only ~8% (w_A 0.273 -> 0.252), the RW decrement and stability are unmoved, no latch -- **the strong-edge core (w>=5) carries all circuit function; fan-in density is not a learning-visibility lever**, validating the assembly cut. Honest bounds: single session/seed; the f1r scaling uses the pool-size ratio as an approximation; the DAN->KC / KC->DAN weak tails (+123k/+136k) sit on inactive damped legs -- activating synaptic-level US delivery is their battlefield (direction, not this round) | bci/fanin1 |
| **Two compartments, two rewards: independent associations + state-dependent value expression** | ✅ | (26) **comp2** (`bci/comp2`): `--plastic-comp-reward` generalizes to a comma list of TYPE:GAIN specs (per-pool gate gains: PAM07 14 cells / 4,582 edges at 0.16; PPL103 2 cells / 7,166 edges at 0.02 -- the two compartments' gated edges are fully disjoint), per-compartment private monitors (own r0 freeze + logs), mod = sum of per-compartment vectors; the dan trace now records the per-edge MAX (a vector sum would scale meaninglessly with the gated-edge count). Protocol: A+@PAM07 x3 -> B+@PPL103 x4, with C-pretrained controls, under both gates. Results: (1) **independent associations hold under BOTH gates** -- the B-learning ratio (trained/control) is 1.00 (comp) and 0.99 (glob): zero crosstalk in sequential A->B training; (2) **the over-blocking prediction is refuted at the design level**: MBMD value feedback is **spike-driven and state-dependent** -- A's V expresses only while A is present (in p2 A is absent, A's MBONs return to baseline, no suppression; the two arms' mod_ms are bitwise identical); **over-blocking requires A/B CO-presence = comp1's already-measured blocking paradigm**; (3) **costs and benefits run in parallel**: compartment-gated associations are weak (dw_A 0.004 vs 0.18; dw_B 0.022 vs 0.44) but mutually independent and location-specific. **A circuit-level criterion: the compartment architecture matters under stimulus co-presence; in sequential paradigms the aggregate gate is naturally crosstalk-free**. Honest bounds: PPL103 has only 2 cells (Poisson noise enters the monitor); single seed; small comp-arm magnitudes; also fixed meta.json being overwritten by a partial-resume run (now merges) | bci/comp2 |
| **Re-arm ceiling substrate: d(t) vs R(t) decouple** | ✅ | (27) **sleep6** (`bci/sleep6`): five single-ignition trials (sleep5 dose/seed) truncated exactly at sleep5's delay moments; the trial-end per-edge d (std-debug dump) IS the resource state at each R(t) measurement (deterministic prefix = identical dynamics). **STATE ceiling nailed**: at 80 s the ever-depleted edges (84.7% of the CEN_C pool) have d recovered to **0.976** while R_mbon stays capped at 0.41 -- **the ceiling is the plateau state, not the resource** (sleep5's interpretation quantitatively confirmed); the d equilibrium at 0.975-0.976 is analytically the plateau's residual firing (d_eq ~ 0.976 => ~16 Hz effective rate over depleted edges; a 2.4% resource deficit, negligible against the 55% response loss); d(t) and R(t) fully diverge after 35 s = response capacity is a network-state variable. **The sleep2-6 chain closes**: the slow-STD resource carries only the "extinguish" half (self-depletion kills the up state); the "maintain suppression" half is carried by the plateau state itself (resource nearly full yet capped) -- the hysteretic pair's two states live on different variables: up = resource + recurrence attractor, down = network-state suppression. Honest bounds: single seed; d is a whole-pool statistic; the molecular substrate of the state suppression is unlocalized (per-population rates + current decomposition = the next question) | bci/sleep6 |
| **Global use_sign: impact audit + flip pilot + recalibration boundary** | ✅ | (28) **sign1** (`bci/sign1`): phases A/B of the queue item 5. **Phase A audit**: the central-brain recurrence CEN_C has **38.4% of rows / 40.0% of synaptic weight INHIBITORY yet running excitatory** (a fidelity gap since exp017 -- it explains the spontaneous ictogenesis and the 0.002 working point), CEN_R 11%, OLR_V 10%; ORN_C/CEN_V/KCM have no inhibitory rows (no-ops). `--use-sign-all` implemented (6 groups flipped, salted key). **Phase B pilot** (sz_l protocol): (1) spontaneous ictogenesis is fully abolished at the calibrated gains (0.004-0.008 quiet vs the base arm's self-ignition latch at 10.4 uV); (2) the latch band moves ~3.5x higher and is NON-MONOTONIC (0.012 marginal, 0.014 latches 3/3 seeds at 492 Hz, 0.016 an inhibition-quench window, 0.032 latches) -- the old "0.0035->0.004 hard bifurcation, no controllable middle" was an inhibition-misplacement artifact, but the fix does NOT produce a controllable middle band; (3) **the recalibration boundary (Phase C blocker)**: the odor response collapses 34x (+0.151 -> +0.0044 uV) and NO gain inside the stable window recovers it (0.010/0.012 still -30x while 0.012 already destabilizes at 8.7 uV) -- the all-excitatory recurrence WAS a ~30x signal amplifier. **Conclusion: global sign is not a drop-in switch; it is a new model variant needing its own calibration program**; follow-ups = odor-loop recalibration under sign / the quench-window mechanism / per-group enablement as an intermediate-fidelity option | bci/sign1 |
| **dstate1: the odor-DC state vs the seizure plateau** | ✅ | (29) **dstate1** (`bci/dstate1`): the two lines' means agree to three decimals (mmdev2's odor DC -1.825/0.179 vs the sleep plateau -1.825/0.18), suggesting one state. **Strong-form unification REFUTED**: a single DA1@150 pulse (60 s, 2 seeds) shows the odor-DC state is a **deep-quiescence lock** -- mean -1.8206 (matches) but **std 0.0145 = 1/7 of dark** (the plateau: 0.18 = 1.8x dark), MBON/KC/DAN all at 0.0 Hz (plateau MBON 3-5), ALPN 19.8 = half of dark (plateau 38), zero decay over 60 s -- **two different network states with the same mean**. Discriminators = std + ALPN (0.015/20 vs 0.18/38). dstate characterized: **an odor-triggered baseline-quiescence axis** (switch threshold far below the fast-response threshold; BCI-relevant). Open: whether the shared mean reflects a common upstream sensory-cascade persistent adaptation (adding ORN to pop-rate tests it); exit conditions untested. Honest bounds: one dose, 2 seeds; ORN not monitored | bci/dstate1 |
| (29 addendum 2) **dstate2: the quiescence lock is recurrence-mediated and narrow** | ✅ | **dstate2** (`bci/dstate2`): a CEN_C gain ladder attributes the ALPN self-sustainment -- 0.00005/0.001 no state (dark return), **0.002 the quiet lock** (ALPN alone at 19.9 Hz), **0.003 a multi-population attractor** (ALON 365 / ALLN 338 / ALIN 73 / MBON 167 / ALPN 149 / KC 47 Hz, |mean| 6.46). **The sustainment is recurrence-mediated, not ALPN-intrinsic**; the AL local circuit (ALON/ALLN loudest) is the shared engine; **a three-step state ladder** (no state -> quiet lock -> broad attractor -> 0.004 full seizure latch) -- the chem working point 0.002 sits exactly on the cliff edge of the first attractor, giving mmdev2's "switch threshold far below the fast-response threshold" its mechanism (fast response and lock formation are two different legs, and the working point hugs the lock threshold). Follow-ups: a fine lock-window scan + an AL-local split surgery. Honest bounds: single seed, four gain points | bci/dstate2 |
| (29 addendum 3) **dstate3: lock window ~0.0016-0.0023, AL-local engine confirmed** | ✅ | **dstate3** (`bci/dstate3`, new `--al-local-gain` surgery: the AL's internal rows split out of CEN_C into an ALL group): (1) fine scan -- formation threshold 0.0015-0.0018, transformation threshold 0.0022-0.0025 (MBON jumps 0 -> 92), **lock window ~0.0016-0.0023** (~0.0007 wide, working point in the upper half); dstate1 observation corrected -- inside the lock ALPN is NOT alone: ALLN/ALON are equally active (46/32 Hz; dstate1's pop-rate set missed the AL local classes). (2) **Engine CONFIRMED**: ALL killed (0.0001) -> the lock dies completely (dark return); neutral (0.002) -> exact replication (ALPN 19.8); boosted (0.004) -> the broad attractor AT the working point (ALON 396 / MBON 63). **Two-engine picture**: the lock = the AL-local engine (PN<->LN self-sustaining); the central engine joins at ~0.0024 (attractor -> seizure) -- dstate2's three steps are two engines igniting in sequence; the working point rides the AL engine's cliff edge (same background as sign1's fragility). Follow-ups: lock-window sensitivity to population size; the two-engine reading of the ALPN-rate difference (20 vs 38). Honest bounds: single seed; the transformation threshold's half-width unscanned; ALL sign kept latent | bci/dstate3 |
| (29 addendum 4) **dstate4: the matching matrix -- asymmetric divergence** | ✅ | **dstate4** (`bci/dstate4`): un-confounds dstate1's three variables (entry / gain / slow-STD gate) with a 2-entry x 2-gain + STD-matched matrix (60 s; two arms reused as deterministic prefixes): (1) **asymmetric divergence** -- the entries converge only at full gain (odor@0.004 -> full latch, MBON 395, same as ignition); at the working point they diverge maximally: odor -> the quiet lock (ALPN 19.9), **ignition -> DARK with zero trace** (600 pA direct LAL current forms no lock at 0.002); (2) **the lock is orthodromic-path-specific**: central strong drive cannot prime the AL loop at 0.002 -- the "switch threshold far below the fast-response threshold" applies to the olfactory orthodromic path; (3) **the slow STD bites the AL engine** (odor@0.002+STD -> ALPN 20 -> 7.7, mean retreats halfway to dark) -- the AL-local rows sit inside the CEN_C pool and are STD-modulatable (correcting dstate3's "central zero-participation" wording); the sz 38 Hz plateau = ignition latch -> STD-driven collapse -> the AL engine's 38 Hz remnant at 0.004. **Entry path is a third state variable**; un-confounded, the two states are genuinely different. Honest bounds: seeds bound to entries; no STD pair for ign_g002; heuristic state labels | bci/dstate4 |
| **sleep7: localizing the substrate of the plateau's response cap** | ✅ | (30) **sleep7** (`bci/sleep7`, pure analysis, zero acquisition): a per-population INCREMENT decomposition of sleep5's delay points (p2 pulse vs the plateau baseline [D-8,D-2] s) localizes the cap: **ALPN +0.7~1.7 Hz (PINNED -- the same drive reaches 120 Hz in the naive network) -> KC +6 Hz (the PN->KC leg is dead: 40 Hz of self-sustained ALPN sustains only a 0.1 Hz KC baseline, same family as condit3's ALK suppression) -> MBON +60 Hz (13x its own 5 Hz baseline -- the readout responds robustly)**. The "45% cap" is therefore NOT an MBON or resource failure (sleep6 excluded the resource) but the upstream compound of AL-engine pinning + KC-cascade failure; the pinning substrate is the same AL-local engine as the dstate series -- one more shared-mechanism link between the two suppressed states. The "network-state suppression" is now fully localized at the model level. Honest bounds: single-seed existing files; the driver of the KC +6 unlocalized; the parameter realization of the AL pinning not disassembled | bci/sleep7 |
| **comp3: co-presence dual reward -- completing the 2x2** | ✅ | (31) **comp3** (`bci/comp3`): fills the missing cell of the comp series (A co-present x B's own reward x BOTH compartment US firing; new chem `compABd`; the glob reference arm is protocol-bitwise-identical to comp1's global arm -> deterministic reuse, zero acquisition). **Both pre-registered predictions hit**: comp arm B ratio **1.00** (r0 1.03/r1 0.97) -- A's V is co-expressed as usual but structurally invisible to PPL103's private gate (the compartments' gated edge sets are disjoint); glob arm **0.68** (comp1's blocking channel applied to B's OWN reward). Mechanism arithmetic closes: glob p2 gate-integral ratio 0.67 = dw ratio 0.676; per-trial traces show blocking is a TIMING effect -- trained mod sits flat at ~660 ms from p20 (A's V already present) while control decays 1778->646 (co-present A closes the gate's first half); comp arms' mod traces coincide trial by trial. Secondary finding: co-presence half-closes even the private gate (mod_ms ~390 vs comp2's single-odor ~1950 -- 2-odor untrained MBON feedback suppresses the DAN pools), but equally for trained/control (part of WHY independence holds); architecture-independent cost of compound stimuli. Full 2x2: sequential (comp2) glob 0.99/comp 1.00; co-presence w/o B reward (comp1) glob 1.41x; **co-presence dual reward (comp3) glob 0.68/comp 1.00** -- the 'when are compartment gates needed = stimulus co-presence density' criterion now holds on the reward side too. Honest limits: single seed x2 sessions (ratio spread <0.06); w_B is a whole-pool subtype mean (diluted); glob arm reused, no fresh seeds; PPL103 has 2 cells | bci/comp3 |
| **kcdrv1: the plateau's KC +6 Hz driver -- the reward channel takes over** | ✅ | (32) **kcdrv1** (`bci/kcdrv1`, pure analysis, zero acquisition): closes sleep7's open question (central direct vs ALPN residual) with causal ORDERING -- lagged xcorr of KC against the 1-ms population traces flips with state: naive phase **ALPN leads KC by 5 ms (r=0.96)** and KC leads MBON (the standard feed-forward cascade); plateau phase **DAN leads KC by 60-80 ms (r~0.9) and MBON by 15-20 ms (r~0.94)** while ALPN retains only r 0.38-0.49. Wiring localization (CEN_C table): **DAN->KC has 5,608 rows, 100% excitatory, mean w 6.4 (weight sum 36k)** -- the synaptic-level US-delivery channel exists at strong-edge scale; MBON->KC is only 258 rows, 96% inhibitory (rules out a direct read of the MBON lead); LAL->KC only 21 rows (the pulse never reaches KC directly). Conclusion: the plateau KC leg is not dead, it SWITCHES -- the response channel changes from sensory (ALPN->KC, suppressed by the CEN_C working point) to reward (DAN->KC, outside the suppressed set); the same KC population enables a different input leg depending on network state; the plateau fly-man answers the world through its value system, not its sensory system -- complementing condit3's 'PN->KC working-point suppression'. Honest limits: single seed; xcorr locking is not synaptic causal proof (but wiring direction + the naive-phase counterexample + exclusion form a triangulation); onset thresholds are not cross-population comparable (peak-normalized) -- ordering follows xcorr; resolution +/-5 ms | bci/kcdrv1 |
| **Stimuli: real image input** | ✅ | general visual-input module: `visual_inputs.json` catalog + grayscale video → ommatidia sampling (blur/normalization/per-eye full-frame eye-plane mapping) + synchronized dual view on the page | docs/STIMULUS.md §5 |
| **Stimuli: chemosensation (olfaction + taste)** | ✅ olfaction validated / taste peripheral validated | new regions `olfactory`/`gustatory` (ORN 2,639 / GRN 1,428; edges ORN_C 57k pairs → antennal lobe, GRN_C 8.4k → SEZ); `--chem-input` pulse protocol; ORN/GRN transduction cascade (shared visual kinetics); calibration finding: the central brain at default recurrence gain is an ignition attractor (82 Hz self-sustained), chem working point CEN_C→0.002 restores antennal-lobe-specific decaying responses; **olfactory scalp signature ×1.4–1.65**; taste signature weak (×1.04, 800 pA negative) because most GRN output targets the VNC reflex arc (not projected) | exp019, docs/SENSORY_INPUTS.md |
| **Stimuli: proprioception + motor-intention decoding** | ✅ done (v1+v2) | new region `proprioception` (PRO 1,454, rootSide L/R groups; PRO_V→VNC 47.9k pairs, PRO_C→central 4.8k); MI-v1: cue decodable (99–100%, 0.3% of background, p=0.002, pure PRO_C terminal dipole), no delay persistence (first-pass band 78–89% was a filtfilt long-tail artifact); **MI-v2: DN pathway probe** (96% of DN's 3.92M input synapses already simulated: cb_intrinsic/LAL 61%, DN→DN 13%, ascending AN 13%; PRO direct only 0.27%) → imagery-window internal drive (LAL* routed 1200 pA / DN direct 800 pA control, 12 trials each): **imagery window decodable throughout** (routed +0.001–0.002 uV p≤0.013; direct −0.005–0.008 uV p≤0.002, glutamatergic polarity flip), no ignition | exp020, docs/SENSORY_INPUTS.md §4 |
| **Stimuli: hearing** | ⏸ planned | JO+AMMC present in data, untouched by the community | docs/SENSORY_INPUTS.md §5 |
| 64/128/241-channel exact waveform re-export | ⏸ pending finalization | ~3-8 min per configuration with GPU+caches (first kernel build dominates; EGI241 slowest), command in USAGE §6 | — |

## 3. Hyperparameters: where they live and how to change them

| Location | Contents | How to modify |
|---|---|---|
| `src/ffbm/params.py` (SECTIONS registry) | all model hyperparameters: LIF, synapses, phototransduction, MT, operating point, VPN/central brain, human four-shell head, **stimulus protocols** (epoch table/contrast/drift speed/seeds etc.) | edit directly; `python -m ffbm.params` regenerates docs/PARAMS.md (a test keeps the doc from going stale) |
| `params.cal()` | runtime CAL dict (key names are the override entry points, resolved in order) | programmatic override: `cal = dict(fp.CAL); cal["I_R_BASE"]=...` |
| `viz/data/elec_configs.json` | electrode-configuration catalog (45/64/128/EGI241 + custom) | regenerate with `viz/make_elec_configs.py`, or manually append `kind:"custom"` entries |
| `viz/data/elec_layout_1010.json` | the 45-channel layout used for current exports | `viz/make_elec_layout.py` (consumes the JSON exported by the calibration panel) |
| Browser localStorage | working state of the page's three calibrations (calib1020 / viewDirs / headFormCalib_v3) + electrode-editing drafts | adjust directly with page sliders/handles; "Copy JSON" exports them |
| `viz/export_data.py` CLI | `--elec-layout`, `--regions` | command line |

Meaning of the status labels (third column of each row in PARAMS.md):
dataset=given directly by the dataset |
literature=value from the literature | calibrated=fitted to measured
waveforms | phenomenol.=phenomenological construction |
assumed=working assumption | numerical=algorithmic choice (validated) |
chosen=design choice | deferred=open item.

## 4. Documentation index

| Document | Contents |
|---|---|
| HANDOFF.md | handoff: environment recovery, data facts, API reference, bug history, domain survey |
| docs/TECHNICAL.md | methodology: all the math from connectome → circuit → simulation → forward kernel → background EEG |
| docs/PARAMS.md | hyperparameter registry (auto-generated, do not hand-edit) |
| docs/STIMULUS.md | stimulus protocols: implementation, parameters, real-image feasibility |
| docs/USAGE.md | this manual: page operation, calibration workflows, export process |
| docs/data.md | data-file documentation |
| viz/ELEC_CONFIGS.md | electrode-configuration file format + description of the four configurations |

## 5. Known limitations

- Stimuli are vision-only: synthetic protocols (1-D texture line scan +
  full-field flicker) and real video input are both implemented
  (visual_inputs.json catalog); before using real video as a formal
  "experimental paradigm", review the mapping-limitation notes in
  docs/STIMULUS.md (normalization-statistics conventions etc.).
- R7/R8 (color vision) are not assembled; color-vision input first requires
  adding the ommatidial R7/R8 circuitry.
- The cervical VNC participates in the dynamics only and does not project to
  the scalp (it lies outside the magnified head sphere — physically correct).
- 64/128/241 channels currently show interpolated preview waveforms; the
  exact export will be run once things are finalized (last row of §2).
- EGI 256 is the vendor's geodesic geometry (E numbering has no anatomical
  meaning) and does not fully coincide with the rule-derived 10-x cap
  (expected behavior).
